# Matte Nuke Plugin

Nuke 15 C++ plugin scaffold for refined image matting with BiRefNet.

当前仓库先把主框架搭好：
- Nuke 原生节点骨架
- LibTorch / TorchScript 推理后端接口
- BiRefNet TorchScript 模型接入位
- 输入图像 -> alpha/mask 输出的基本流程

现在的节点已经走整块采样、整块推理和按行回填的主路径；你后面主要需要准备可用的 TorchScript 模型，并确认 `torchvision/_C.so` 能在 Nuke 进程里被加载。

## Planned Architecture

```text
Nuke Input RGBA
    -> BiRefNetMatteIop
    -> frame/tile sampling
    -> BiRefNetTorchBackend
    -> LibTorch / TorchScript module
    -> mask / alpha output
```

## Project Layout

```text
.
├── CMakeLists.txt          # CMake 入口，转到 CMakeList.txt
├── CMakeList.txt           # 主要构建配置
├── include/
│   └── BiRefNetPlugin/
│       ├── BiRefNetMatteIop.h
│       ├── BiRefNetTorchBackend.h
│       └── MatteTypes.h
└── src/
    ├── BiRefNetMatteIop.cpp
    ├── BiRefNetTorchBackend.cpp
    └── plugin.cpp
```

## What Is Already Wired

- `BiRefNetMatte` 节点已经预留这些参数：
  - `model_path`
  - `torchvision_ops_library`
  - `use_gpu`
  - `input_width`
  - `input_height`
  - `unpremult_input`
  - `clamp_input`
  - `passthrough`
  - `mask_threshold`
- CMake 已支持：
  - Nuke 15 SDK / `DDImage`
  - 从本机 Python `torch` 包自动发现 `TorchConfig.cmake`
  - Torch 运行库 `rpath`
  - `torchvision/_C.so` 默认路径发现
- `BiRefNetTorchBackend` 已经留好两个关键入口：
  - `initialize(...)`
  - `infer(...)`

## Next Steps For Real BiRefNet

你后面主要补这几块就行：

1. 从 BiRefNet Python 权重导出 TorchScript。
2. 在 Nuke 里确认 `torchvision/_C.so` 路径可用。
3. 根据素材测试：
   - `input_width`
   - `input_height`
   - `unpremult_input`
   - `clamp_input`
4. 拿真实素材联调 TorchScript 输出 shape 和 matte 质量。

## Build Notes

先改这两个路径：

- `NUKE_ROOT`
- `TORCH_CMAKE_PREFIX`（如果自动发现失败）

然后正常走 CMake。当前仓库里新增了标准入口文件 `CMakeLists.txt`，这样 IDE 和命令行都能直接识别。

## Important Note

Nuke 节点现在已经是 TorchScript 推理骨架，但真正上线前还需要你验证两件事：

- 你的 TorchScript 导出是否稳定保留了 BiRefNet 的最终输出结构
- `torchvision` 自定义算子在 Nuke 进程里是否能正常注册和执行
